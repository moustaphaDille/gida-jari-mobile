name: gida_jari_mobile
description: "Gida Jari — La Maison de Demain. Application mobile Flutter pour les profils Client, Artisan et Architecte/Ingénieur."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/gida-jari.jpeg
